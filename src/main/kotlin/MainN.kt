import rules.one_time.applyOneTimeRules

fun main() {
    findSolution(Week())
}

fun findSolution(week: Week) {
    applyOneTimeRules(week)
    //week.print()

    //applyRules(week)

    //If is solution return

    //If is not solution, set a random value and apply the rules again
}